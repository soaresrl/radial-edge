import Point from "../../geo/point";
import Region from "../region";
export default class Cube {
    static generate(region: Region, center: Point, width: number, height: number, depth: number): void;
}
